# 函数参数需求文档

> **目的**：定义严辰乐（后端核心）需要其他组员提供的函数、接口、数据格式。
> **原则**：严辰乐的代码中用到的所有"别人负责的函数"，都在这里约定清楚签名和返回值。
> **日期**：2026-06-08

---

## §1 约定

- **Python 环境**：`D:\Anaconda\python.exe`（Python 3.13.9）
- **所有数据库操作函数**返回 `Optional` 或 `List`，不直接抛异常（异常由调用方统一处理）
- **所有 SQL 查询**使用参数化查询，禁止字符串拼接（防 SQL 注入）
- **日期时间格式**：统一使用 ISO 8601 字符串 `"2026-06-08T15:30:00"` 或 `"2026-06-08"`

---

## §2 苏文韬 需要提供的函数

### 文件路径约定

严辰乐从以下路径导入：
```python
from database.db_service import (
    get_user_by_username,
    check_user_exists,
    insert_user,
    get_db_connection,
)
```

### 2.1 数据库连接

```python
def get_db_connection() -> pymysql.Connection:
    """
    返回一个 MySQL 数据库连接。
    连接参数从 config.py 读取（MYSQL_HOST, MYSQL_PORT, MYSQL_USER,
    MYSQL_PASSWORD, MYSQL_DATABASE）。

    返回:
        pymysql.Connection — 已建立连接的数据库对象

    注意:
        - 使用 autocommit=False，由调用方决定是否 commit
        - 连接使用完后调用方负责 close()
    """
    ...
```

### 2.2 用户查询（认证用）

```python
def get_user_by_username(username: str) -> Optional[User]:
    """
    根据用户名查询系统用户。

    参数:
        username: str — 用户名

    返回:
        User 对象（包含 user_id, username, password_hash, role, status）
        None — 用户不存在

    SQL 示例:
        SELECT user_id, username, password, role, status, created_at
        FROM user WHERE username = %s
    """
    ...
```

### 2.3 检查用户名是否存在

```python
def check_user_exists(username: str) -> bool:
    """
    检查用户名是否已被注册。

    参数:
        username: str — 用户名

    返回:
        True — 已存在
        False — 不存在
    """
    ...
```

### 2.4 插入新用户

```python
def insert_user(user: User) -> int:
    """
    向 user 表插入一条新用户记录。

    参数:
        user: User 对象（含 username, password_hash, role, status=1）

    返回:
        int — 新插入用户的 user_id（自增主键）

    SQL 示例:
        INSERT INTO user (username, password, role, status, created_at)
        VALUES (%s, %s, %s, 1, NOW())
    """
    ...
```

### 2.5 数据查询 API（第4天起，先约定签名）

```python
def query_sales(
    start_date: str,
    end_date: str,
    region: Optional[str] = None,
    category_id: Optional[int] = None,
    channel: Optional[str] = None,
    group_by: str = "day",
) -> dict:
    """
    多维度销售数据查询（UC-01）。

    参数:
        start_date: "2025-01-01"
        end_date:   "2025-12-31"
        region:     None=全国, "广东", ...
        category_id: None=全品类, 1, 2, ...
        channel:    None=全渠道, "PC", "Mobile", "Miniprogram"
        group_by:   "day" | "week" | "month"

    返回:
        {
            "summary": {
                "total_sales": 1234567.89,     # 总销售额（元）
                "total_orders": 5678,           # 总订单量
                "avg_order_value": 217.45,      # 平均客单价
                "change_rate": 0.123            # 环比变化率（12.3%）
            },
            "series": [
                {"date": "2025-01-01", "sales": 12345.67, "orders": 56},
                ...
            ],
            "by_category": [
                {"category_name": "服装", "sales": 500000, "percentage": 0.40},
                ...
            ],
            "by_region": [
                {"region": "广东", "sales": 300000, "percentage": 0.24},
                ...
            ]
        }
    """
    ...
```

### 2.6 报表导出（第4天起）

```python
def export_report(
    report_type: str,       # "daily" | "weekly" | "monthly" | "custom"
    params: dict,           # 报表参数（时间范围、维度等）
    format: str,            # "pdf" | "excel" | "png"
) -> str:
    """
    生成报表并返回下载文件路径。

    返回:
        str — 生成的文件路径（如 "/tmp/report_20260608_153000.pdf"）
              调用方负责将此路径转为下载 URL
    """
    ...
```

---

## §3 薛淞 需要提供的函数

### 文件路径约定

严辰乐从以下路径导入：
```python
from ml.sales_predict import predict_sales
from ml.stock_predict import predict_stock
from ml.anomaly_detect import detect_anomalies, check_rule
from ml.user_profile import compute_profile
```

### 3.1 销售趋势预测（UC-02）

```python
def predict_sales(
    product_ids: Optional[List[int]] = None,  # None = 全品类
    category_id: Optional[int] = None,        # None = 全品类
    forecast_days: int = 30,                  # 预测天数：7/30/90
    model_type: str = "auto",                 # "auto" | "moving_avg" | "linear" | "arima"
) -> dict:
    """
    基于历史销售数据，预测未来 N 天的销售额。

    数据来源:
        从 MySQL sales_record 表读取历史数据（由苏文韬的 get_db_connection 提供连接）

    返回:
        {
            "model_used": "arima",             # 实际使用的模型
            "mape": 0.152,                     # 历史拟合 MAPE（15.2%）
            "confidence": "high",              # "high" | "medium" | "low"
            "forecast": [
                {
                    "date": "2026-06-09",
                    "predicted_sales": 45678.90,
                    "lower_bound": 41000.00,    # 95% 置信区间下界
                    "upper_bound": 50000.00,    # 95% 置信区间上界
                },
                ...  # 共 forecast_days 条
            ],
            "turning_points": [
                {"date": "2026-06-15", "note": "预计达到销售峰值", "value": 52000.00},
                ...
            ]
        }

    注意:
        - 如果历史数据 < 12 个月，在 confidence 字段标记为 "low"
        - 如果所选品类数据波动过大，在返回中附带 warning 字段
    """
    ...
```

### 3.2 库存需求预测（UC-03）

```python
def predict_stock(
    product_ids: List[int],
    forecast_days: int = 30,
) -> dict:
    """
    基于历史销量 + 当前库存 + 季节因子，预测库存需求。

    返回:
        {
            "products": [
                {
                    "product_id": 1,
                    "product_name": "XX商品",
                    "current_stock": 500,
                    "predicted_demand": 620,        # 未来30天预测需求量
                    "safety_stock": 150,            # 安全库存线
                    "suggested_replenish": 270,      # 建议补货量
                    "replenish_window": "2026-06-10~2026-06-15",  # 建议补货窗口
                    "turnover_days": 24.2,           # 当前库存周转天数
                    "risk_level": "warning"          # "safe" | "warning" | "danger"
                },
                ...
            ],
            "summary": {
                "safe_count": 45,       # 安全库存 SKU 数
                "warning_count": 12,    # 预警库存 SKU 数
                "danger_count": 3       # 缺货风险 SKU 数
            }
        }
    """
    ...
```

### 3.3 异常检测（UC-08）

```python
def detect_anomalies(
    scan_time: str,  # 扫描时间 "2026-06-08T15:00:00"
) -> list:
    """
    定时扫描最新销售数据，检测异常并返回告警列表。

    检测规则:
        - 销售额突降：某品类/全品类当日销售额较7日均线下降 > threshold%
        - 库存警戒：某 SKU 库存 < 安全库存线
        - 退货飙升：退货率较30日均值上升 > threshold%

    返回:
        [
            {
                "rule_id": 1,
                "rule_name": "全品类销售额突降告警",
                "rule_type": "sales_drop",
                "anomaly_value": -35.2,        # -35.2% 即下降35%
                "baseline_value": 45000.00,     # 正常基线
                "current_value": 29160.00,      # 当前值
                "alert_content": "全品类销售额较7日均线下降35.2%",
                "suggested_action": "建议排查是否有大促后回落或商品下架",
                "severity": "red"               # "red" | "orange" | "yellow"
            },
            ...
        ]

    如果没有异常，返回空列表 []。
    """
    ...


def check_rule(rule_id: int, product_id: Optional[int] = None) -> dict:
    """
    手动触发单条规则的检测（用于用户手动触发或测试）。

    返回:
        {
            "triggered": True,
            "rule_id": 1,
            "details": { ... }  # 同 detect_anomalies 中的单条格式
        }
    """
    ...
```

### 3.4 用户画像计算（UC-04，P1）

```python
def compute_profile(
    customer_ids: Optional[List[int]] = None,  # None = 全部客户
) -> dict:
    """
    计算用户画像标签。

    返回:
        {
            "overview": {
                "total_customers": 50000,
                "high_value_pct": 0.15,
                "mid_value_pct": 0.35,
                "low_value_pct": 0.50,
                "new_customer_pct": 0.22,
                "active_customer_pct": 0.68
            },
            "profiles": [
                {
                    "customer_id": 1,
                    "value_level": "高价值",
                    "avg_order_price": 350.00,
                    "purchase_frequency": 3.2,       # 月均购买次
                    "preferred_categories": ["服装", "美妆"],
                    "promo_sensitivity": "高",
                    "tags": ["高客单价", "促销敏感", "复购率高"]
                },
                ...
            ]
        }
    """
    ...
```

---

## §4 闫维岳 需要对接的前端页面

### 4.1 统一 API 响应格式

所有后端 API 返回的 JSON 遵循统一格式：

```json
// 成功
{
  "success": true,
  "data": { ... },       // 具体数据
  "message": "操作成功"
}

// 失败
{
  "success": false,
  "data": null,
  "error": "错误描述",
  "code": "ERROR_CODE"   // 前端据此处理不同错误类型
}
```

### 4.2 认证 Token 传递

前端在每次请求的 Header 中携带：
```
Authorization: Bearer <jwt_token>
```

Token 过期（401）时前端自动跳转登录页。

### 4.3 严辰乐负责的 API（供闫维岳参考）

| 方法 | 路径 | 说明 | 需要 Token |
|------|------|------|-----------|
| POST | /api/auth/login | 登录 | 否 |
| POST | /api/auth/register | 注册用户 | 是（admin） |
| GET | /api/auth/me | 当前用户信息 | 是 |
| POST | /api/predict/sales | 触发销售预测 | 是 |
| GET | /api/predict/sales/{task_id} | 查询预测结果 | 是 |
| POST | /api/predict/stock | 触发库存预测 | 是 |
| GET | /api/alert/rules | 获取预警规则列表 | 是 |
| POST | /api/alert/rules | 创建预警规则 | 是（admin） |
| GET | /api/alert/logs | 获取预警日志 | 是 |
| PUT | /api/alert/logs/{id}/resolve | 标记预警已处理 | 是 |

---

## §5 姚凯曦 需要对接的工作

### 5.1 测试数据

- 从 MySQL 导出 `sales_record` 子集（约 1000 行）作为功能测试数据
- 补充边界场景模拟数据：空数据、超大数据量、异常值（负数/零/极大值）

### 5.2 前端页面（管理类）

- 登录页：调用 `POST /api/auth/login`
- 用户管理页：调用用户 CRUD API（待苏文韬提供）
- 预警展示页：调用 `GET /api/alert/logs`
- 预警处置：调用 `PUT /api/alert/logs/{id}/resolve`
- 数据源配置页：调用数据源 API（待苏文韬提供）

---

> **下一步**：苏文韬/薛淞/闫维岳/姚凯曦各自认领自己的 § 节，按签名实现。
> **问题**：函数签名有疑问找严辰乐讨论，确认后锁定不再改。
